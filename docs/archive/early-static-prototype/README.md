# Early static landing-page prototype

This directory preserves the pre-Next.js landing-page draft that was present in the worktree before the production application foundation was scaffolded.

It is not part of the active build. The production application starts in the repository-level `app/` directory.
